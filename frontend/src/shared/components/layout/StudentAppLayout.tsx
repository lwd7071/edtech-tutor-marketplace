'use client';
import WorkspaceLayout from './WorkspaceLayout';
export function StudentAppLayout({children}:{children:React.ReactNode}) { return <WorkspaceLayout role="STUDENT">{children}</WorkspaceLayout>; }

